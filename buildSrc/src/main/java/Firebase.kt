object Firebase {

    private const val authVersion = "21.0.1"
    const val auth = "com.google.firebase:firebase-auth-ktx:$authVersion"

    private const val analyticsVersion = "21.0.0"
    const val analytics = "com.google.firebase:firebase-analytics-ktx:$analyticsVersion"

    private const val safetyNetVersion = "16.0.0"
    const val appCheck = "com.google.firebase:firebase-appcheck-safetynet:$safetyNetVersion"

    private const val messagingVersion = "23.0.5"
    const val messaging = "com.google.firebase:firebase-messaging:$messagingVersion"
}